{#
/**
 * @file
 * Description.
*/
#}

{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
] %}

{% if content %}
  <div{{ attributes.addClass(classes) }} aria-live="polite" aria-atomic="true">
    {{ content }}
  </div>
{% endif %}
